services:

  # ================================
  # React + Nginx
  # ================================
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080/api}
    restart: unless-stopped
    ports:
      - "80:80"
